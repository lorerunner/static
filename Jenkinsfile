pipeline{
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello world Build"'
            }
        }

        stage('Upload to AWS') {
            steps {
                sh 'echo "Hello world Upload to AWS"'
            }
        }
    }
}
